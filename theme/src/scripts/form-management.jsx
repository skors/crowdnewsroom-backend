import React from "react";
import ReactDOM from "react-dom";
import FormDetails from "./form-details";

const rootElement = document.getElementById("form-management");
ReactDOM.render(<FormDetails />, rootElement);
